<?php
/**
 * 開発環境のデータベース接続設定のひな形。
 *
 * このファイルを db.php にコピーする。README の手順通りにセットアップしていれば
 * この値のままで動く。パスワードを変えた場合は、README の CREATE USER / ALTER USER
 * のSQLも同じ値に揃えること。
 * db.php 本体は接続情報を含むためコミットしない。
 */

return array(
	'default' => array(
		'type'       => 'pdo',
		'connection' => array(
			'dsn'      => 'mysql:host=localhost;dbname=kakeibo;charset=utf8mb4',
			'username' => 'kakeibo',
			'password' => '12345678',
		),
		'charset'   => 'utf8mb4',
		'profiling' => true,
	),
);
